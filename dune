(executable
  (name main)
  (public_name wayland-proxy-virtwl)
  (preprocess (pps ppx_cstruct))
  (libraries wayland wayland.protocols virtio_gpu cmdliner cstruct-lwt fmt x11))

(vendored_dirs ocaml-wayland)
